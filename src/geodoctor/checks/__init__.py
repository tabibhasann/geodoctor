"""Geometry, CRS, schema, structure, and topology checks.

Each submodule registers its checks as a side effect of import.
"""

from . import crs, geometry, schema, structure, topology  # noqa: F401  side-effect import
